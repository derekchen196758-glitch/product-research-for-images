---
name: product-research-for-images
description: 美工产品调研助手 - 根据统一输入格式（主ASIN、补充ASIN、产品类型、产品英文名称，配件类可补充车型/机器信息、OEM编号、真实素材链接），调用 Sorftime MCP 进行高效调研，生成单份汇总型 CSV 报告（核心卖点、用户痛点、图片建议方向、风险提醒、目标客户等），供后续 ChatGPT 出图方案使用。触发条件：用户提供产品任务信息并要求调研、生成 CSV、产品分析等。**独立运行**，不依赖其他任何 Skill。
---

# 美工产品调研助手

> **独立性说明：** 本 Skill 只依赖 Sorftime MCP 的 `product_detail` 和 `product_reviews` 两个工具，不依赖其他任何 Skill。只要当前环境有 Sorftime MCP 即可直接使用。

## 目标

本 Skill 的任务不是简单“抓几个卖点”，而是稳定地为后续图片策划和 Prompt 生成提供高质量输入。

输出应服务于以下问题：

1. 这是什么产品
2. 买家最关心什么
3. 哪个卖点最适合做主卖点图
4. 哪些痛点适合做安装图 / 对比图 / 尺寸图
5. 哪些场景不适合直接 AI 生图

## 统一输入标准

只使用 **一套输入标准**。美工不需要区分普通产品版和配件产品版，Skill 内部自动处理。

### 推荐输入字段

- `主ASIN`
- `补充ASIN`
- `产品类型`
- `产品英文名称`
- `车型/机器信息`
- `OEM编号`
- `可参考真实素材链接`

### 实际处理规则

1. **所有任务默认都接收同一套字段**
2. **普通产品**
   - 核心依赖：`主ASIN`、`补充ASIN`、`产品类型`、`产品英文名称`
   - `车型/机器信息`、`OEM编号`、`可参考真实素材链接` 可为空
3. **配件产品**
   - 优先使用完整字段
   - 如果缺少 `车型/机器信息` 或 `OEM编号`，继续执行，但必须在输出中标记 `【需确认】`
4. **若未明确提供产品类型**
   - 如果输入中出现明显的适配信息 / OEM 编号，则按配件产品处理
   - 否则按普通产品处理，并在输出中标记 `【产品类型需确认】`

### 推荐输入格式

```text
产品类型：配件产品
产品英文名称：11535 Oil Filter
主ASIN：B0XXXXXXX
补充ASIN：B0AAAAAAA, B0BBBBBBB
车型/机器信息：Compatible with Ford F-150 2015-2020
OEM编号：68157291AA, 68436631AA
可参考真实素材链接：https://...
```

普通产品输入时，后 3 项可不填。

## 工作流

### Step 1: 解析输入

从用户输入中提取：

- 主ASIN
- 补充ASIN（可多个，用英文逗号分隔时需拆分）
- 产品类型
- 产品英文名称
- 车型/机器信息
- OEM编号
- 可参考真实素材链接

处理规则：

- 自动去重 ASIN
- 第一个有效 ASIN 视为主ASIN
- 其余视为补充ASIN
- 默认使用 US 站点

### Step 2: 主ASIN优先校验

先检查主ASIN是否满足基本可用性：

- `product_detail` 能否正常返回
- 标题是否存在
- 五点描述 `mainFeatures` 是否存在
- 评分 / 评论数是否存在

若主ASIN详情严重缺失：

- 继续执行，但在输出中加入 `风险提醒：主ASIN信息不足`
- 不要把补充ASIN机械平替成主ASIN

### Step 3: 高效抓取策略

为了兼顾成功率和效率，按以下顺序抓取：

#### 3.1 主ASIN必抓

始终抓取主ASIN：

```text
product_detail(main ASIN)
product_reviews(main ASIN, Positive)
product_reviews(main ASIN, Negative)
```

#### 3.2 补充ASIN按需抓取

对补充ASIN，优先只抓 `product_detail`。

只有在以下情况之一出现时，才补抓补充ASIN评论：

- 主ASIN五点描述太弱
- 主ASIN评论太少
- 主ASIN卖点和品类判断不清晰
- 需要补充痛点或场景方向

这样可以减少无效调用，提高整体成功率。

### Step 4: 提取与判断

#### 4.1 从产品详情中提取

从 `product_detail` 返回结果中提取：

- `title`
- `brandName`
- `starRating`
- `ratingsCount`
- `monthSales`
- `price`
- `mainFeatures`
- `categoryName`

#### 4.2 从评论中提取

从正评中提取：

- 用户认可的功能
- 用户实际购买动机
- 哪些卖点被重复验证

从负评中提取：

- 用户痛点
- 尺寸 / 安装 / 结构 / 适配类问题
- 哪些问题容易影响图片表达

#### 4.3 主次权重规则

- 主ASIN是主分析对象，权重最高
- 补充ASIN只用于补充，不做平权平均
- 如果主ASIN与补充ASIN信息冲突：
  - 以主ASIN为主
  - 冲突内容写入 `风险提醒`
  - 不擅自融合成确定结论

### Step 5: 面向图片流程的分析

除常规卖点外，额外判断：

1. 哪个卖点最适合做主卖点图
2. 哪些痛点适合做尺寸图 / 安装图 / 对比图
3. 哪些场景可以直接 AI 生图
4. 哪些场景不适合直接 AI 生图

重点判断以下高风险情况：

- 小众配件
- 复杂安装位
- 特定车型 / 机器适配关系
- OEM / 适配信息不足
- 评论明显指出“尺寸不对”“不匹配”“安装错误”

若命中以上情况，在输出中明确提醒：

- `建议真实素材 / 买家秀 / 视频截帧 / PS合成辅助`

## 输出规则

### 输出形式

生成 **单份汇总型 2 列 CSV**，不要按 ASIN 分别生成多份结果。

CSV 文件保存到工作目录，命名为：

`ProductResearch_<主ASIN>_YYYY-MM-DD.csv`

### 输出字段

| A列（标题） | B列（内容） |
| --- | --- |
| 产品基础信息 | 产品类型、产品英文名称、主ASIN、补充ASIN、配件补充信息 |
| 核心卖点（高置信度） | 只保留有明确依据的最多3条卖点，每条标注来源 |
| 用户痛点 | 最多3条，优先保留和图片表达强相关的问题 |
| 图片建议方向 | 哪张图更适合强调什么，重点围绕买家判断问题 |
| 风险提醒 | 数据不足、适配不明、场景易画错、评论不足、信息冲突等 |
| 目标客户 | 主要受众画像 |

### 输出内容要求

#### 1. 产品基础信息

内容示例：

```text
产品类型：配件产品；产品英文名称：11535 Oil Filter；主ASIN：B0XXXXXXX；补充ASIN：B0AAAAAAA, B0BBBBBBB；车型/机器信息：Compatible with Ford F-150 2015-2020；OEM编号：68157291AA, 68436631AA
```

#### 2. 核心卖点（高置信度）

- 最多 3 条
- 必须有明确来源
- 每条末尾标注 `(五点)`、`(好评)` 或 `(五点+好评)`
- 不得因为标题看起来像卖点就直接写入

#### 3. 用户痛点

- 最多 3 条
- 优先输出会影响图片方向的痛点，例如：
  - 安装难
  - 尺寸易错
  - 接口易裂
  - 适配不清晰

#### 4. 图片建议方向

必须围绕“帮助买家判断”来写，而不是只写“做一张好看的图”。

建议至少包含：

- 白底图应突出什么
- 卖点图应打哪个主卖点
- 尺寸图 / 安装图适合强调什么
- 场景图应该体现什么真实用途
- 是否建议使用真实素材辅助

#### 5. 风险提醒

至少覆盖以下异常：

- 主ASIN信息不足
- 评论样本不足
- 五点描述过弱
- 配件类适配信息不足
- 小众产品场景不适合直接 AI 生图
- 补充ASIN与主ASIN存在信息冲突

若无明显异常，可写：

`未发现明显高风险项`

#### 6. 目标客户

基于产品功能、评论语境、使用场景推测主要人群，不要凭空写过细画像。

## 示例输出

```csv
产品基础信息, 产品类型：配件产品；产品英文名称：11535 Oil Filter；主ASIN：B0XXXXXXX；补充ASIN：B0AAAAAAA, B0BBBBBBB；车型/机器信息：Compatible with Ford F-150 2015-2020；OEM编号：68157291AA, 68436631AA
核心卖点（高置信度）, ①内置磁铁吸附金属杂质(五点) ②高密度滤纸过滤更稳定(五点) ③安装方便、替换效率高(好评)
用户痛点, ①接口尺寸容易选错 ②塑料接口易裂 ③部分用户反馈安装位不够清晰
图片建议方向, 白底图突出接口与结构细节；主卖点图优先表现过滤结构；尺寸图重点回答接口和规格；安装图建议明确安装位置；场景图避免纯AI硬画复杂安装位，必要时用真实素材或截图辅助
风险提醒, 配件类产品，若车型/机器信息与OEM不完整，适配图和安装图需标记【需确认】；复杂安装场景不建议完全依赖AI生图
目标客户, 自行保养车辆用户 / 需要替换滤芯的维修用户 / 注重兼容性的配件购买者
```

## 红线规则

1. **卖点真实性**：只输出有明确文本依据的高置信度卖点，不可自行脑补
2. **来源标注**：卖点必须标注 `(五点)` / `(好评)` / `(五点+好评)`
3. **不确定标注**：任何不确定、冲突、缺失的信息标注 `【需确认】`
4. **数据不足处理**：若详情或评论不足，明确写 `【数据不足】`，不要虚构
5. **单份输出**：默认只生成一份汇总 CSV，不按多个 ASIN 拆成多份
6. **品牌侵权红线**：图片建议方向中禁止建议直接展示竞品品牌 Logo、品牌图标或品牌名称视觉元素
7. **场景真实性红线**：对复杂安装位、小众配件、特殊车型场景，不得默认建议“直接 AI 生图即可”

## 处理约定

- **独立运行**：本 Skill 不依赖其他任何 Skill，只需 Sorftime MCP 可用即可
- **直接执行**：接收任务信息后直接按工作流执行，不询问用户确认
- **输入优先级**：优先识别结构化字段；若用户只给一句自然语言，也应尽量解析出主ASIN、补充ASIN和产品类型
- **多ASIN处理**：补充ASIN仅用于补充，不得覆盖主ASIN结论
- **效率优先**：补充ASIN评论仅在主ASIN信息不足时再抓，避免无效调用
- **稳定性优先**：宁可输出 `【需确认】`，也不要输出看似完整但不可靠的结论

## 与 SOP 的衔接

生成的 CSV 文件直接拖入 ChatGPT，配合后续图片方案 Prompt 使用：

- 普通产品：重点支持白底图、卖点图、尺寸图、使用场景图策划
- 配件产品：重点支持适配图、OEM图、安装图、场景图风险判断

本 Skill 的输出应帮助后续明确：

- 哪个卖点最值得主打
- 哪些痛点适合做图解释
- 哪些场景不应纯靠 AI 硬画
